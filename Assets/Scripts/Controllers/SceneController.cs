using System.Collections;
using System.Collections.Generic;
using UnityEngine;
public static class SceneController
{
    // idea: array of scenes, then nextScene() prevScene() goToScene(), resetScene(go back to start) 

    public static bool nextScene()
    {
        Debug.Log("next scene");
        bool successful;
        successful = true;
        return successful;
        
    }
}
